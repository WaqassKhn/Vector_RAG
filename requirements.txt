# Core ML / Embeddings
sentence-transformers>=2.2.2
numpy>=1.24.0
scikit-learn>=1.3.0
torch>=2.0.0


# Vector Store
faiss-cpu>=1.7.4          # kept for RAGBench eval harness (temp in-memory VDB per sample)
pinecone>=3.0.0           # primary production vector database

# LLM APIs
google-genai>=0.1.1       # Gemini — last-resort fallback
httpx>=0.24.0             # OpenRouter HTTP client (async-capable)
sseclient-py>=1.7.2       # SSE streaming for OpenRouter token-by-token responses

# Retrieval
rank-bm25>=0.2.2          # BM25 sparse retrieval (local reranking layer)

# Document Parsing
pdfplumber>=0.10.0
pypdf>=3.17.0
python-docx>=1.1.0
pandas>=2.0.0

# Application
streamlit>=1.30.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Testing
pytest>=7.4.0
